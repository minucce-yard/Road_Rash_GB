Writing to VRAM sometimes takes too much time, damaging the sky tiles on top of screen.
-  Engine tries to do too much in a short time.


VBlank handler must run much better.
-  Take advantage of CPU registers when writing VRAM data.
-  Split transfers based on HUD updates of 4 frames.
-  Run priority PPU data first.
-  Simulate DMG STAT bug for faster processing.
